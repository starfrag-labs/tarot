{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "tarot-core.fullname" . }}-pdb
  labels:
    {{- include "tarot-core.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "tarot-core.selectorLabels" . | nindent 6 }}
{{- end }}
