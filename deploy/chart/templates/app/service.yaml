apiVersion: v1
kind: Service
metadata:
  name: {{ include "tarot-core.fullname" . }}
  labels:
    {{- include "tarot-core.app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tarot-core.app.selectorLabels" . | nindent 4 }}
